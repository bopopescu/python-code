CREATE TABLE `user` (
`id` int(2) NOT NULL AUTO_INCREMENT,
`name` varchar(20) NULL,
PRIMARY KEY (`id`) 
);
CREATE TABLE `user_copy_1` (
`id` int(2) NOT NULL AUTO_INCREMENT,
`name` varchar(20) NULL,
PRIMARY KEY (`id`) 
);
